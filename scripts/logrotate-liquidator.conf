# Liquidator JSON logs — install under /etc/logrotate.d/ on VM
/path/to/optimism-aave-v3-liquidator-ts/logs/*.log {
    size 100M
    rotate 7
    copytruncate
    missingok
    notifempty
    compress
    delaycompress
}
